{
  "name": "vecsy",
  "private": true,
  "version": "0.5.1",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "vitest run --project unit",
    "test:browser": "vitest run --project browser --browser.headless",
    "test:webkit": "vitest run --config vitest.webkit.config.js --browser.headless"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "4.1.10",
    "playwright": "1.62.1",
    "vitest": "4.1.10",
    "wrangler": "^4.110.0"
  }
}
